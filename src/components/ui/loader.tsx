import { Loader2 } from "lucide-react";

const Loader = () => {
  return (
    <div className="w-screen flex items-center justify-center min-h-screen bg-background">
      <Loader2 />
    </div>
  );
};

export default Loader;
